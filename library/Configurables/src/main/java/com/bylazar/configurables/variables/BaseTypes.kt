package com.bylazar.configurables.variables

enum class BaseTypes {
    INT,
    LONG,
    DOUBLE,
    STRING,
    BOOLEAN,
    FLOAT,
    ENUM,

    UNKNOWN,
    UNSUPPORTED,
    RECURSION_REACHED,
    JSON_ERROR,
    ERROR,

    CUSTOM,
    ARRAY,
    LIST,
    MAP,
    GENERIC,
    GENERIC_NO_ANNOTATION
}